import styled from "styled-components";

export const MediumButtonWrapper = styled.button`
	background-color: var(--mainRed);
	padding: 0.5em;
	border: none;
	border-radius: 4px;
	width: 33%;
	height: 15vh;
	margin: 0 1em;
	color: var(--mainWhite);
	@media (max-width: 500px) {
	}
`;
